fastapi==0.115.0
uvicorn[standard]==0.30.6
# Pulled transitively by uvicorn[standard]; declared explicitly because
# the live per-share log streamer (backend/log_streamer.py) imports it.
websockets>=12.0
httpx==0.27.2
aiosqlite==0.20.0
pydantic==2.9.2
pydantic-settings==2.5.2
PyYAML==6.0.2
python-multipart==0.0.12
pywebpush==2.0.0
cryptography==43.0.1
jinja2==3.1.4
psutil==6.0.0
# Async MQTT client for the optional Home Assistant / ESPHome publisher
# (backend/mqtt.py). Optional at runtime: the publisher self-disables if
# this import fails, so an install without it still runs fine.
aiomqtt==2.5.1
